#include "main.h"

/**
 * _isupper - thi is to checks for uppercase character
 * @c: it is the character to be checked
 * Return: return 1 if c is uppercase, 0 otherwise
 */
int _isupper(int c)
{
return (c >= 'A' && c <= 'Z');
}
