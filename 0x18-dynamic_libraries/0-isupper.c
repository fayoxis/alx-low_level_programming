#include "main.h"

/**
 * _isupper - checks if the character is  uppercase character
 * @c: this is the  character to be checked
 * Return: returns 1 if c is uppercase, 0 otherwise
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
