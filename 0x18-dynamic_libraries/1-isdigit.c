#include "main.h"

/**
 * _isdigit - it checks for a digit (0 through 9)
 * @c: thi is the int to be checked
 * Return: returns  1 if c is a digit, 0 otherwise
 */
int _isdigit(int c)
{
return (c >= '0' && c <= '9');
}
